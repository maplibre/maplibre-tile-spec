export function decodeString(buffer: Uint8Array, offset: number, numStreams: number, presentStream: Uint8Array, numValues: number): [number, Uint8Array, String[]] {
    // TODO
    return [offset, presentStream, new Array(numValues)];
}
